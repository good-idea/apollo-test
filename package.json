{
	"name": "apollo-root",
	"version": "0.0.1",
	"description": "Root package.json for Apollo",
	"repository": "git@github.com:sanctuarycomputer/apollo",
	"author": "Sanctuary Computer",
	"license": "NONE",
	"scripts": {
		"setup": "yarn install && brew tap shopify/shopify && brew install shopify-cli && yarn login",
		"login": "yarn workspace apollo-shopify login --store $STORE_DOMAIN"
	},
	"workspaces": [
		"api",
		"shopify"
	],
	"private": true,
	"devDependencies": {
		"dotenv": "^10.0.0",
		"dotenv-cli": "^4.1.0",
		"typescript": "^4.5.2"
	}
}
